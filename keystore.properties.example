# Keystore configuration for signing the release APK
# Copy this file to keystore.properties and fill in your actual values

# Path to your keystore file (can be relative or absolute path)
KEYSTORE_FILE=path/to/your/keystore.jks

# Password for the keystore
KEYSTORE_PASSWORD=your_keystore_password

# Key alias in the keystore
KEY_ALIAS=your_key_alias

# Password for the key
KEY_PASSWORD=your_key_password
